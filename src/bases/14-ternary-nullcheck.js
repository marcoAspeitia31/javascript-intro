let firstName
let lastName = 'Aspeitia'

// Null check
// console.log(`${ firstName || 'No firstName'} ${ lastName || 'No lastName' }`)

const isActive = true

// Ternarios
const message = ( isActive === true ) ? 'Activo' : 'Inactivo'

console.log(message)

